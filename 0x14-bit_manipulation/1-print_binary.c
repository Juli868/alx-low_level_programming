#include "main.h"
/**
 * print_binary - will print the equivalent value of a number
 * @n: number to find its equivalen binary
 */
void print_binary(unsigned long int n)
{
	unsigned int remain, res; 

	remain = n;
	while (remain > 2)
	{
		res = remain
		print
}
