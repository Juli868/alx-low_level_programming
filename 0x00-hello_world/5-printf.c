#include<stdio.h>
/*
 * main() - is the starting of the program
 *Return: shows the end with 0 if succeded
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
