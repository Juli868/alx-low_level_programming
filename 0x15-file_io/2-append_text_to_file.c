#include "main.h"
/**
 * append_text_to_file- adds a string to a cureent one in a file
 * @filename:name of the file
 * @text_content: what to add
 * Return: 1 if succeeded
 */
int append_text_to_file(const char *filename, char *text_content)
{
	int file;

	if (filename == NULL)
		return (-1);

}
