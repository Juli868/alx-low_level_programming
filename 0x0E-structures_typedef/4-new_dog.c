#include "dog.h"
#include <stdlib.h>
/**
 *
 *
 *
 */
dog_t *new_dog(char *name, float age, char *owner)
{	
	dog.name = name;
	dog.age = age;
	dog.owner = owner;
	if (dog->name == "NULL" && dog->owner == "NULL")
	{
		return;
	}
	return (dog);
}
